{
  "name": "examtarget",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:web": "pnpm --filter web dev",
    "dev:engine": "cd apps/engine && uvicorn main:app --reload --port 8000",
    "build": "pnpm --filter web build"
  },
  "devDependencies": {}
}
